---
// index.astro — the homepage (route: "/").
// For Phase 1 this just proves the layout + theme work. Real sections
// (hero, about, projects, etc.) come in later phases.
import Layout from "../layouts/Layout.astro";
---

<Layout title="John Jacobson — Portfolio">
  <main class="container" style="padding-block: var(--space-6);">
    <h1>John Jacobson</h1>
    <p>
      Foundation in place. This is a temporary placeholder confirming the dark
      theme, layout, and global styles are working.
    </p>
  </main>
</Layout>
