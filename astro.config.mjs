// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // `site` is your final public URL. Astro uses it to build absolute links
  // (e.g. for sitemaps and social-preview meta tags later).
  site: "https://johnrjacobson.com",
});
